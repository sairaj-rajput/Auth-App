<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css"
    rel="stylesheet"
/>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <div class="main w-full bg-black text-white p-12">
    <div class="flex justify-end w-full pr-10">
      <a class="bg-red-600 px-3 py-2 rounded-md text-md mb-5 inline-block" href="/logout">Logout</a>
    </div>
    <h3 class="text-3xl mb-2">
      <span class="font-light">Hello</span>, <%= user.name %>👋
    </h3>
    <h5 class="mb-5">You can create a new post...</h5>
    <form action="/post" method="post">
      <textarea class="block resize-none w-1/3 p-3 outline-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" placeholder="what's on your mind ?"></textarea>
      <input class="block px-3 py-2 bg-blue-600 w-40 mt-3 rounded-md text-md" type="submit" value="Create New Post" />
    </form>
    <div class="posts mt-12">
      <h3 class="text-zinc-400">Your Posts.</h3>
      <div class="post-container mt-5">
        <% user.posts.reverse().forEach((post) =>{  %>
        <div class="post w-1/3 p-4 mb-4 bg-zinc-800 rounded-md border-[1px] border-zinc-800">
          <div class="flex justify-between">
          <h4 class="text-blue-400 mb-2">@<%= user.username %></h4>
          <a class="text-lg text-zinc-400 hover:text-red-400" href="/delete/<%= post._id %>"><i class="ri-delete-bin-6-line"></i></a>
          </div>
          <p class="text-sm tracking-tight"><%= post.content %></p>
          <div class="btns flex gap-6 mt-5">
            <a class="text-xl text-red-500" href="/like/<%= post._id %>"><i class="ri-heart-3-line text-white"></i><i class="ri-heart-3-fill"></i></a> <small><%= post.likes.length%></small>
            <a class="text-xl text-zinc-400 hover:text-blue-500" href="/edit/<%= post._id %>"><i class="ri-edit-box-line"></i></a>
          </div>
        </div>
    <%    }) %>
      </div>
    </div>
  </div>
</body>

</html>